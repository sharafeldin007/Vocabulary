import Foundation
import SwiftUI

extension UI.Navigation {
  struct View: Screen {
    var body: some Screen {
      HStack {
        Image(systemName: "chevron.left")
          .padding()
      }
    }
  }
}
